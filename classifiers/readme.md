This notebook contains the conventional classifiers. The features from the text are extracted using TF_IDF
vectorization which are used to classify into the given classes. All the classification is done on Spam-Ham 
dataset. You can use different dataset and can easily compare the results.